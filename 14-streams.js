// STREAMS => read and write seqeuentially
// writeable => write data sequentially 
// readable => read data sequentially
// duplux => both read and write data sequentially
// transform => when data can be modificed while writing or reading

// streams extend event emitters